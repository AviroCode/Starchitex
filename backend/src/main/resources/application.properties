# Spring Boot Database Configuration
spring.datasource.url=jdbc:postgresql://dpg-d97ltieq1p3s73f25b1g-a.singapore-postgres.render.com/starchitex
spring.datasource.username=starchitex_admin
spring.datasource.password=fXVqm3V18feETBc10WM3SThHivIfLpqf

# Spring Data JDBC (Manual SQL) configuration
spring.jdbc.template.query-timeout=30s

# Disable schema initialization (schema already exists in Render DB)
spring.sql.init.mode=never

# Enable SQL debug logging
logging.level.org.springframework.jdbc.core.JdbcTemplate=DEBUG
logging.level.org.springframework.jdbc.core.StatementCreatorUtils=TRACE
