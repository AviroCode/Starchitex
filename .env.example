# Copy to .env (gitignored) and fill in real values before running
# `docker compose up`. Generate strong values with:
#   openssl rand -base64 32                    # JWT_SECRET
#   openssl rand -base64 24 | tr -d '/+='       # POSTGRES_PASSWORD
POSTGRES_PASSWORD=
JWT_SECRET=
